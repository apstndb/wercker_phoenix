build:
  box: aktsk/docker-elixir:phoenix-build
  steps:
    - script:
        name: echo env
        code: env
test:
  box: aktsk/docker-elixir:phoenix-build
  steps:
    - script:
        name: get deps
        code: mix deps.get
    - script:
        name: test
        code: make build build-static test test-npm
